package com.cielo.repository;

import com.cielo.model.TotalControleLancamento;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TotalControleLancamentoRepository
        extends JpaRepository<TotalControleLancamento, Integer> {
}
