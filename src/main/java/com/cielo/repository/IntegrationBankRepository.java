package com.cielo.repository;

import com.cielo.model.IntegrationBank;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IntegrationBankRepository extends JpaRepository<IntegrationBank, Integer> {
}
